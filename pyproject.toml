[project]
name = "astrbot-plugin-qq-group-auditor"
version = "0.1.1"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "pyyaml>=6",
]
