[project]
name = "astrbot-plugin-qq-group-auditor"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "pyyaml>=6",
]
