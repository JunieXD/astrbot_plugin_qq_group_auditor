# Changelog

## v0.4.1 - 2026-09-25

- 接入 AstrBot 日志并保存审核阶段明细，区分模型排队/返回、账号等待/恢复、在线检查与平台操作耗时；详情沿用群管理员权限。重载保留 v3 共享队列与未知操作保护，记录失败不阻止审核。
- 新增仅保存最终输出、精简原始 usage 的独立开关；旧数据库自动迁移，缺失字段仍为未知。
- 支持 CREDITS 额度、按配置模型名匹配价格、ECNU 北京时间峰谷倍率和缓存未知时的费用区间；调用开始冻结价格配置，不重复计算思考 token。
- 提供四非提示词，补充真实日志发现的 SDUT / SDUST / HBUT 映射，并附真实申请回归样本。

## v0.4.0 - 2026-09-22

- 新增本插件专用模型选择及 ECNU JSON Schema、思考开关、思考程度配置；使用请求级 Provider / SDK 配置副本，解决 AstrBot 丢弃生成参数的问题，不修改共享 Provider。保留原生代理、认证、超时、重试和用量统计。
- ECNU Plus / Max 按模型校验思考档位；不支持的适配器或组合明确报错。实时审核、补审和测试共享最多 3 个并发模型调用，重载取消等待中的请求。
- 提供非985非211审核规则示例，补充历史中出现的学校简称、曾用名与歧义处理说明。

## v0.3.1 - 2026-09-22

- 移除审核模型调用中固定的 `max_tokens=512`，输出长度遵循 AstrBot 与模型提供商的配置；保留 DeepSeek JSON 模式请求参数。

## v0.3.0 - 2026-09-22

- 新增本插件的逐次 LLM 调用统计，记录实时审核、补审及测试的模型、用量、缓存命中/未命中、思考 token、耗时、结果与重试关联；兼容 DeepSeek 原始缓存字段。用量缺失或矛盾时保留未知，不按零消耗计费。
- 新增可配置的模型单价、币种、正文记录开关和保留天数，保存每次调用使用的价格快照；思考 token 作为输出明细展示，不重复计费。统计独立保存在 `llm_usage.sqlite3`，写入失败不阻止审核，重载取消和进程中断保留对应状态。
- 新增管理员私聊 `/qgaudit stats [7d] [群号|all] [模型名]` 和 `/qgaudit export ...`，支持加权缓存命中率、平均/P95 耗时、分币种费用及 CSV 导出；申请详情显示关联调用。查询和导出仅包含操作者有权限管理的群。
- 调整提示词顺序为固定系统要求与输出示例、群规则、群号、答案、申请人 QQ；重试提示追加在末尾以保留相同前缀。申请字段使用 JSON 字符串编码，避免换行伪造额外字段。

## v0.2.11 - 2026-09-21

- 修复 NapCat 普通申请列表使用 `invitor_uin` 返回申请人时，重载中断或停机遗漏的申请被补审跳过：平台适配层统一申请人字段，兼容 `requester_uin`、`user_id` 和 `invitor_uin`；仅对 `join_requests` 使用兜底，不混淆真正邀请事件中的邀请人。
- 补审继续遵守审核规则、群配置的随机等待、账号间隔、状态检查和去重；缺少申请人 QQ 的系统记录输出明确跳过原因。增加两条申请在等待审批时被重载中断的回归测试，覆盖重新加载后恢复、人工处理后跳过和重复同步不重复审批。

## v0.2.10 - 2026-09-21

- 多群任务改为在已到期任务中优先审批，并按群轮转；名片和通知随等待时间提高优先级，避免低优先级任务长期得不到处理。实际操作仍遵守账号共享间隔、离线恢复和失败冷却。
- 每群新增审核延迟上下限，默认 `-1` 继承全局；主动申请、邀请和补审共用本群设置。新安装的全局默认改为 5–15 秒，保留已有明确配置。
- 增加入队、等待原因、开始执行和跳过原因日志；重载时原地升级共享调度对象，保留跨插件锁、间隔、冷却与未知操作保护。

## v0.2.9 - 2026-09-21

- 修复名片任务的 30–90 秒等待阻塞后续邀请和申请审批：各任务在账号锁外等待，到期后再竞争执行；实际操作仍串行，并从上一次操作完成后计算账号间隔。
- 入群通知、审批后补偿与历史补处理共用成员锁，拿到锁后重新读取审计状态；同一成员已经完成的名片任务不再重复等待和查询。入群事实先落库，不受名片等待影响。
- 账号恢复等待改为共享且持久化，仍保留离线暂停、失败冷却、提交前记录与未知结果保护；取消尚未执行的任务不再留下长时间的空队列占位。
- 热重载时原地升级共享调度对象，保留其他插件持有的引用、锁及异常类型；与 GitHub 订阅插件同步调度修复。

## v0.2.8 - 2026-09-21

- 修复重复邀请或重复申请导致旧 flag 失效的问题：持久化关联同平台、同群、同申请人的请求，在审核前、模型返回后和审批延迟结束后检查新请求、人工处理及入群状态。旧请求标记为 `superseded` 或 `handled`，不再审批或发送过期忽略通知。
- 精确识别 NapCat 的 `No such request`，记录为 `unavailable`，不重试、不发送平台异常通知、不计入账号连续失败。权限、网络和未知结果仍保留原有异常及冷却保护。
- 后台同步先保存整批状态再补审；保存原始 flag，兼容同秒重复、乱序和延迟事件。NapCat 系统消息没有请求时间时从其序号恢复时间，避免把同步时间当成申请时间。
- 同 flag 补充答案后使用新内容审核，停止旧的空答案或邀请决定；新请求在问题和昵称查询前落库，避免查询期间旧请求仍被审批。
- 平台审批成功后立即记录结果，避免等待通知或重载插件时丢失审批结果。数据库自动升级到版本 4，保留原有申请、成员和处理记录。

## v0.2.7 - 2026-09-12

- 新增可配置的审核、邀请、名片、管理员通知延迟；与 GitHub 订阅共享账号排队、失败冷却及重载后仍有效的节奏记录。
- 审批请求发出前记录操作标识；结果未知时不盲目重复。数据库不可用时暂停自动审核，等待中已被他人处理的申请不再提交审批。
- 名片修改改为等待后读取真实成员状态，只修改空名片；去掉短时间反复写名片来确认入群的行为，补偿也覆盖已入群但名片操作暂缓的记录。
- 群问题、昵称查询增加缓存；后台同步增加可配置随机间隔。保持原始空评论的邀请识别规则及普通空答案的 30 秒等待。

- 后台群系统消息同步由每分钟改为默认每 10 分钟，可关闭或调整间隔；查询历史与定时任务共用限频和互斥，避免叠加轮询。
- 同步前检查本地 OneBot 连接和 QQ 在线状态；掉线或同步失败后逐步延长重试间隔，默认最长 1 小时，并输出简短原因。移除根据历史平台 ID 发起轮询的回退行为。

- 普通空答案申请先等待 30 秒；同平台、同群、同一申请人在窗口内补充答案时立即审核新请求，旧空答案仅保留替代流水，不再发送忽略通知或调用审批接口。
- 重复空答案不延长等待，超时只处理最后一条；实时和补审共享等待逻辑，卸载时取消后台等待任务，同标识补充答案时更新未审核的审计记录。

## v0.2.6 - 2026-08-27

- 增加每群独立的 `invite_action`，支持对受邀入群直接通过、忽略或拒绝，默认忽略且全程不调用 LLM。
- 受邀请求的三种结果复用现有通知开关并完整写入审计；直接通过的受邀成员不会进入实时、定时或历史群名片处理。
- 针对 NapCat 未暴露邀请类型的限制，仅将原始申请附言完全为空的实时请求识别为受邀入群，并保留普通“有问题但答案为空”的原处理逻辑。

## v0.2.5 - 2026-08-25

- 插件启动、更新或重新启用后，自动补审停机期间遗漏且平台仍标记为未处理的加群申请。
- 已处理、已审核、停用群和邀请类记录不会补审；实时审核与补审增加申请级互斥，防止重复调用 LLM 和审批接口。
- 每轮最多补审 10 条，每次实际调用 QQ 审批接口前随机等待 2 至 5 秒并逐条隔离异常；补审审计流水标记为 `plugin_catch_up`。

## v0.2.4 - 2026-08-25

- 历史补处理和后台补偿改为逐成员隔离失败；成员已退群时跳过当前成员，不再中断整批任务。
- 后台自动设置名片的尝试次数持久化，每次申请最多 5 次，重启插件后也不会无限重试。
- 每次实际调用群名片设置接口前随机等待 0.8 至 2.2 秒，批量操作保持串行间隔。
- 审计记录中已知存在非空群名片时不做修改，并在详情中展示直接设置尝试及失败原因。
- 保持不调用当前 NapCat 版本会报错的群成员信息和成员列表接口；无法可靠读取的未知历史名片不会伪装成已检测。

## v0.2.3 - 2026-08-25

- 将 NapCat 的“群成员不存在”识别为尚未入群或已经退群，不再作为自动修改名片故障通知管理员。
- 自动补偿仅扫描最近 15 分钟内审批或外部处理的申请；定时扫描只尝试一次，不再为陈旧审批反复创建密集重试任务。
- 历史补处理单独统计“当前不在群内”和其他平台设置错误。

## v0.2.2 - 2026-08-25

- 自动改名片改用申请快照和 `get_stranger_info` 获取昵称，直接调用 `set_group_card`，不再调用 NapCat 的 `get_group_member_info` 群成员缓存接口。
- 历史补处理不再调用 `get_group_member_list`；按每个 QQ 最新的有效通过记录限速逐个设置，只有平台确认设置成功后才补记成员会话。
- 合并直接设置成功后迟到的真实入群通知，避免把同一次入群记录两遍；真实通知会补全准确时间和操作者。

## v0.2.1 - 2026-08-25

- 自动批准后主动确认成员状态并设置群名片，定时核对作为后续兜底，不再依赖 NapCat 必须发送 `group_increase`。
- 从 `问题：... / 答案：...` 格式中提取纯答案用于审核、审计和 `{answer}` 群名片占位符，同时保留原始验证消息。
- 增加 `/qgaudit backfill <群号>`，批量补处理数据库有记录、当前仍在群内且未成功设置名片的历史成员。
- 对补偿入群、迟到通知、重复事件和重复 backfill 做幂等处理；当前名片已符合模板时不重复调用平台接口。

## v0.2.0 - 2026-08-25

- 使用 SQLite 保存每一次加群申请、插件审核决定、平台审批操作、实际入群和退群记录。
- 记录实际入群与退群通知中的操作者 QQ，支持同一成员多次申请和多段成员会话。
- 增加默认关闭的自动群名片功能，支持 QQ号、QQ昵称、问题、答案和入群时间占位符。
- 增加 `/qgaudit history` 和 `/qgaudit detail` 私聊审计查询命令。
- 定期同步 NapCat 群系统消息；外部已处理但未观察到入群的申请按拒绝记录，并标记为推断来源。

## v0.1.2 - 2026-08-03

- DeepSeek provider 按官方 JSON Output 接口传入 `response_format={"type":"json_object"}`，并为审核结果预留 512 tokens。
- LLM 返回空内容、无效 JSON 或字段结构错误时自动重试一次。
- 兼容完整包裹 JSON 的 Markdown 代码块；异常响应以转义形式记录，最多保留前 500 个字符。

## v0.1.1 - 2026-07-03

- 修复管理员通知私聊会话默认使用 `aiocqhttp`，导致自定义平台名下通知发送失败的问题。

## v0.1.0 - 2026-06-30

- 初始版本：支持 QQ 加群申请 LLM 自动审核、按群配置、异常私聊通知和私聊测试命令。
